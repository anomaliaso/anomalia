# Widen the homepage product mockup

The homepage chat preview now shows the agent panel already open on desktop. The panel contains a static remote-computer view that makes the agent’s work visible without connecting to a real session.

The mockup grows from 940px to 1200px to fit the chat and panel together. At the existing 860px narrow breakpoint, the panel is removed from the layout and the original chat view remains.
