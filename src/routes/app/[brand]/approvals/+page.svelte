<!--
  Approvals has been merged into Contenuti (the single queue at /content).
  +page.server.ts redirects (308) to /content, so this component never renders.
  Kept only so the /approvals route stays valid for existing links/bookmarks.
-->
