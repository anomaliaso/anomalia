import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-05',
  title: 'Your agent can make a carousel',
  items: [
    'Ask for a carousel and you get a set of slides that look like one piece — same palette, same motif, same light — instead of unrelated pictures.',
    'To change a single slide, your agent refines that slide rather than redrawing the whole set.'
  ]
} satisfies ChangelogEntry;
