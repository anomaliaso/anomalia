// Il formato lo definisce ./index.ts: qui solo i dati.
const entry = {
  date: '2026-08-27',
  title: 'One pipe for text',
  items: [
    'Chat, embeddings, video and audio reviews and music prompts now run through a single AI gateway — one place decides what powers the product, and model swaps are configuration, not code.',
    'Self-hosting: set the six LLM_* variables in your .env or chat, embeddings and reviews fail with a named error telling you exactly what is missing.',
    'Autopilot weeks that take longer than a serverless limit no longer die mid-run: the weekly run is queued for the worker and gets a full hour to finish.',
    'A chat that stalls no longer hangs forever: the agent retries with a fresh session, and abandoned runs are closed cleanly.',
  ],
};

export default entry;
