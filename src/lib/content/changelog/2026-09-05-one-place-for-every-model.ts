import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-05',
  title: 'Every model now goes through one place',
  items: [
    'AI person photos are generated the same way as every other image, so they follow the image model you picked instead of a fixed one.',
    'Planning a month of articles now writes and illustrates them in minutes, and the email arrives when they are ready — the slower 12-24 hour queue is gone, and fast generation is no longer a Pro-only option.',
    'AI visibility still measures whether your brand gets cited in DeepSeek answers.'
  ]
} satisfies ChangelogEntry;
