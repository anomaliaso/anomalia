import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-04',
  title: 'A new home page: your AI writes it, Anomalia publishes it',
  items: [
    'The home page now says what Anomalia actually is: you connect your own Claude or Cursor over MCP, your model plans and writes on your own subscription, and Anomalia checks the platform rules, renders the images, schedules, publishes to your real accounts and reads the results back.',
    'A new section sets out who does what, and every line on the Anomalia side is something the product does today — the 280-character limit on X, no text-only posts on Instagram, video only on YouTube.',
    'The nine channels Anomalia publishes to are now listed from the publisher itself, so the page can never claim a platform it cannot post to.'
  ]
} satisfies ChangelogEntry;
