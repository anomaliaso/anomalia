import type { ChangelogEntry } from './index';

const entry: ChangelogEntry = {
  date: '2026-09-02',
  title: 'New models reach the chat picker on their own',
  items: [
    'When a model provider you already use releases a new model, it shows up in the chat model picker by itself — no update needed.',
    'Models the provider retires leave the picker the same day, so you cannot pick one that no longer answers.'
  ]
};

export default entry;
