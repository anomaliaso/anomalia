import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-03',
  title: 'The model you picked is the model that answers',
  items: [
    'Chats now always run on the model chosen for them, instead of occasionally failing on a different one nobody selected.'
  ]
} satisfies ChangelogEntry;
