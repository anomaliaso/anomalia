import type { ChangelogEntry } from './index';

const entry: ChangelogEntry = {
  date: '2026-09-04',
  title: 'Every SEO/GEO number now shows its evidence',
  items: [
    'Your AI assistant can read every SEO and GEO audit you have ever run, not just the last one — so "why did visibility drop in August?" is answered from the August audit instead of a new one.',
    'Each citation check comes with the engine that answered, the exact question asked, when it ran, and the sites it cited.',
    'The fixes Anomalia generates — FAQ blocks, structured data, llms.txt, landing copy — can now be read in full and published as they are.',
    'These are reads: they cost no credits and cannot change a stored measurement.'
  ]
};

export default entry;
