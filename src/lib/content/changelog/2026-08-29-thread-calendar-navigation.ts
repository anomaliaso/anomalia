import type { ChangelogEntry } from './index';

const entry: ChangelogEntry = {
  date: '2026-08-29',
  title: 'Navigation keeps your place',
  items: ['Opening a post from the calendar and going back now returns to the chat with the calendar modal open']
};

export default entry;
