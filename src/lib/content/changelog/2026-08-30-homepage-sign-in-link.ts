import type { ChangelogEntry } from './index';

const entry: ChangelogEntry = {
  date: '2026-08-30',
  title: 'Sign in from the homepage',
  items: ['The homepage now shows a direct Sign in link, separate from Get started']
};

export default entry;
