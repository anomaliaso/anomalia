import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-06',
  title: 'One tool reads everything',
  items: [
    'Agents now read your brand through a single tool instead of forty — posts, media, articles, memory, competitors, settings and audits all come from the same place.',
    'A long list comes back whole: reads no longer stop short without saying so, and when there is more, the answer tells the agent how to fetch the next page.',
    'Counts are exact when the number is the answer, so "how many posts are waiting" is right rather than estimated.',
    'An article now comes back in full, so an agent that rewrites it starts from your whole text and not a truncated copy.'
  ]
} satisfies ChangelogEntry;
